program name2 (juan, oscar, pablo); 
uses oscar, juan;
label 2132, 96987,23;
const
  Hola = Identifier2;
  Hello = +Identifier3;
  World = -99.99dbg_scale_factor;
  my = 'sdfdjshf';
  name = 'd';
type
  one = string[10];
  two = packed array [1..2] of string[10];
  two1 = packed set of 10..20;
  two2 = file of ^intiger;
  two3 = 
  record
    x,y: real;
    z,w: string;
  end;
  three = ^intiger;
var
  Hola2, Hel3, hal: (one, two, three);
  test: 10..100;
  strtest: string[10];
  Hol1, ksdhf: set of 0..1;

function function1 (var one, two, three: intiger): float;
begin
  one := 20-9;
  func1(a,b,dbg_expression);
  goto 100;
end;

Procedure procedure1 (one: intiger; two: char);
const
  test1 = dbg_signed_number;
begin
  if dbg_expression 
  then
    begin
      cone := a + 8;
      two := dbg_expression;
    end
  else
    b := dbg_expression;

  a := dbg_expression;
  
  case dbg_expression of
    4: goto 10; 
    minus: goto 10; 
    test: goto 10
  end;

  repeat
    one := dbg_expression;
    two := dbg_expression;
  until dbg_expression;

  while x <= n
  do goto 10;

  for Identifier4 := dbg_expression to dbg_expression 
  do
    func(one);

  with Reference.other 
  do
    func2(two);
end;

procedure procedure2;
forward;

procedure procedure3;
external;

begin
  900: test := dbg_expression;
  100: if dbg_expressio
  then
    goto 900;

  test;
  test();
end


